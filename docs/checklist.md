# CMMS 종합 점검 체크리스트

기존 문서(PRD, 파라미터 구조 등)와 이전 작업 내역들을 종합하여, CMMS 시스템 전반에 적용할 수 있는 종합 점검 체크리스트입니다.

## 1. 시스템 점검 항목 (System Checklist)

### 데이터베이스 및 엔티티 (DB & Entity)
- [x] 엔티티의 필드명과 DB 테이블의 컬럼명이 정확히 매핑되었는가? (특히 snake_case 와 camelCase 혼용 문제, 예: `isNotice` vs `is_notice`)
- [x] database와 FE의 snake_case 와 camel_case 구분이 정확한가?
- [x] JSON 항목별로 파싱과 저장이 올바르게 진행되며 데이터 무결성이 유지되는가?
- [x] 모든 테이블에 접근할 때 다중 테넌트 지원을 위한 `company_id`가 빠짐없이 조건에 포함되었는가?

### 백엔드 시스템 로직 (Backend System)
- [ ] ❌ 시스템 전반에서 `companyId` 및 감사 필드(생성/변경 이력)가 항상 누락 없이 관리/저장되는가?
- [x] 사용자 요청 시 하드코딩된 값이 아닌, 실제 로그인한 사용자의 `company_id`가 정상적으로 매핑 및 검증되는가?
- [ ] ❌ 트랜잭션(TX) 및 결재 모듈에서 과정에 따른 상태값 변화(T -> A -> C -> R 등)에 맞춰 업데이트 로직이 정확히 동작하는가?
- [ ] ❌ 상태에 따라 데이터 수정 및 삭제 방지 등 필수 제약 조건이 백엔드 단에서 적절히 적용되는가?
- [x] 트랜잭션(Inspection, Work Order, Work Permit) 모듈에서 Save 동작 시 자동 채번된 ID를 기준으로 마스터(Header)와 디테일(Item)이 함께 묶여 원자적으로 정상 저장되는가?

### 프론트엔드 연동 무결성 (Frontend & Payload)
- [x] API 호출 전 Frontend에서 던지는 Request Body(Payload)키가 Swagger 또는 Backend의 DTO 키와 일치하는가?
- [x] 체크박스(Y/N)나 특정 구분 값 데이터가 Backend에서 500 에러를 내지 않도록 프론트단에서 올바르게 파싱되어 보내지고 있는가?

## 2. 공통 점검 항목 (Common Checklist)

### 인증 상태 및 UI 에러 제어
- [x] 로그인 절차 시 인증(Token) 발급 및 세션 관리가 안정적으로 유지되는가?
- [x] 로그인 후 새로고침 시에도 인증 정보가 유지되거나, 반대로 로그아웃 시 Auth Store(Zustand 등)의 Persist 미들웨어가 완전히 초기화되어 로그인 화면으로 정상 진입하는가?
- [ ] ❌ 필수 값 검증 실패나 서버 API 통신 오류 시, UX 관점에서 어떤 오류인지 명확하게 Toast 메시지나 모달 경고창으로 띄워주고 있는가?

### 화면 기본 흐름 및 UI 제어 (UI/UX)
- [x] Standard(기준정보)는 사용자가 ID를 직접 입력하고, Master Data, TX, Memo, Approval은 자동 채번되어 사용자가 직접 입력하지 못하도록 등록 화면(Register)에서 비활성화(`disabled` 또는 `readOnly`) 제어되어 있는가?
- [x] Memo와 Approval은 사용자의 화면에서 고유 ID가 불필요하게 노출되지 않도록 설계되어 있는가?
- [x] `SearchableSelect`(담당자/부서 검색 콤보박스)가 의도한 대로 동작하고 데이터 바인딩이 정확하게 이루어지는가?
- [x] 4단 Grid Form 구조가 공통 등록/수정 화면 레이아웃 규격에 맞게 시스템 전체에 통일성 있게 배치되었는가?
- [ ] ❌ 버튼 로직(저장, 확정, 상신, 목록, 삭제, 출력)이 상태(Status) 값에 조응하여 적절하게 활성화/숨김 처리되는가?

### 공통 컴포넌트 연동
- [x] 범용 `FileAttachment` 컴포넌트 처리 시 파일들이 Sequence 자동 생성을 통해 올바른 `file_group_id` 로 묶여서 업로드/조회/삭제가 동작하는가?
- [x] Memo와 Approval 작성 시 공통 파일 첨부 기능이 화면상에서 문제없이 연동되어 작동하는가?

## 3. 상세 점검 항목 (Detailed Checklist by Module)

### 3.1. 설비 관리 (Equipment)
**[ 백엔드 ]**
- [x] 설비 마스터 등록 시 고유한 설비코드(Equipment ID)가 채번 정책에 맞게 자동으로 정상 생성되는가?
- [x] `install_location`, `note` 등 추가된 정보가 정상적으로 DB에 저장 및 조회되는가?

**[ 프론트엔드 ]**
- [x] 4단 Grid Form 구조에 맞게 기초/제원/설치 정보 레이아웃이 화면에 적용되었는가?

### 3.2. 예방점검 (Preventive Maintenance - PM)
**[ 백엔드 ]**
- [ ] ❌ 예방점검(Inspection) 결과 입력 시, 대상 설비의 `last_inspection` 갱신 및 `next_inspection` 계산 날짜 등록이 정상 처리되는가? 만일 equipment에서 점검주기가 설정되어 있지 않다면 next_inspection은 null로 저장되는가?
- [ ] ❌ 대상 점검 주기, 최근 점검일, 다음 점검일에 대한 스케줄 계산 로직이 올바르게 동작하는가?
- [ ] ❌ TX에서 `ref_entity`가 EQ, PM, WO, WP 등으로 잘 정의되어 있고, 실적 입력 시 ACT를 기본값으로 하며 `ref_entity`, `ref_id`가 정확히 매핑되고 PLN값을 잘 가져오는가?

**[ 프론트엔드 ]**
- [x] 마스터-디테일 조회가 정상적으로 동작하고 화면 영역별 상세내용을 렌더링하는가?
- [x] 리스트 조회 시 점검 대상 설비 및 날짜 데이터가 의도대로 화면에 올바르게 노출되는가?

### 3.3. 작업지시 (Work Order - WO)
**[ 백엔드 ]**
- [x] 지시 생성 시 작업지시 Item 목록이 마스터(Header)와 함께 묶여서(Atomic) 누락 없이 기입 저장되는가?

**[ 프론트엔드 ]**
- [ ] ❌ 작업 삭제 기능은 UI상에서 임시(T) 상태에서 작성자 본인만 조작할 수 있도록 제어되는가?

### 3.4. 작업허가 (Work Permit - WP)
**[ 백엔드 ]**
- [x] 작업허가서 종류별(Hot, Confined Space 등) JSON 기반 체크시트(`checksheet_json_hot` 등)의 내용이 알맞게 파싱 및 저장되는가?
- [x] 작업허가서에서 각 양식별 체크 항목이 동적으로 생성되며, 결과 데이터가 JSON 형태로 무결하게 저장되는가?

**[ 프론트엔드 ]**
- [ ] ❌ 결재(Approval) 프로세스 진행 상태에 따라 허가서의 유효 및 제어 여부가 화면에 적절하게 반영되는가?

### 3.5. 재고관리 (Inventory)
**[ 백엔드 ]**
- [ ] ❌ 입출고, 이동 등 트랜잭션 발생 시 대상 품목의 마스터 재고 수량(Stock)이 정상적으로 더하거나 빼어지는가?
- [ ] ❌ 재고 증감에 따른 재고 이력(History) 데이터가 트랜잭션 기반으로 안전하게 기록되는가?

**[ 프론트엔드 ]**
- [ ] ❌ 마스터 수량 및 증감 내역이 이력 화면에서 올바르게 호출 및 출력되는가?

### 3.6. 메모 및 공지 (Memo)
**[ 백엔드 ]**
- [x] 메모 신규 등록 시 고유 일련번호(`memo_id`)가 정상 채번되어 `NULL not allowed` 에러 없이 저장되는가?

**[ 프론트엔드 ]**
- [x] `is_notice` 필드('Y', 'N')가 백엔드/프론트엔드 간에 정상 동기화되고, 리스트 UI에서 공지 식별 아이콘/배지가 노출되는가?
- [x] 파일 첨부 기능과 댓글(Comment) 기능이 문제없이 업로드/작성/조회되는가?

### 3.7. 전자결재 (Approval Register)
**[ 백엔드 ]**
- [x] 결재 문서 신규 상신 시 고유 `approval_id` 값이 정상적으로 생성되어 에러 없이 DB에 저장되는가?
- [ ] ❌ 결재 상태 변경을 통해 원본 데이터(WO, WP, Inspection)의 상태가 알맞게 T, A, C, R로 전이되는 백엔드 로직이 정상 작동하는가?

**[ 프론트엔드 ]**
- [x] 프론트 `ApprovalRegisterPage`를 통해 시스템 내 결재 정보(기안자, 결재선지정, 합의/통보자 목록 등)가 프로세스에 맞게 지정 및 정상 제출되는가?

---
💡 프로젝트 진행 상황에 따라 필요한 모듈이나 상세 테스트케이스(TC) 항목을 덧붙여 활용해 주세요!